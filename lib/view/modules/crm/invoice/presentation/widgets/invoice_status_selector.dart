// Placeholder widgets - to be implemented
import 'package:flutter/material.dart';

class InvoiceStatusSelector extends StatelessWidget {
  const InvoiceStatusSelector({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}

class InstallmentList extends StatelessWidget {
  const InstallmentList({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}

class PaymentForm extends StatelessWidget {
  const PaymentForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
